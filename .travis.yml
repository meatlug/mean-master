sudo: false
public: trusty
language: node_js

cache:
  directories:
    - node_modules

node_js:
  - "6"
  - "7"
  - "8"
  - "node"

before_install:
  - npm install npm@latest -g
  - npm cache verify
  - npm prune
  - npm update

install:
  - npm install

script:
  - npm run ci:travis
